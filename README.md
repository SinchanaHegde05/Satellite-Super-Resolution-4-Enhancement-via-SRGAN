# Satellite-Super-Resolution-4-Enhancement-via-SRGAN
Built a GAN-based system that converts low-resolution satellite images (24×24) into high-resolution images (96×96) with 4× upscaling. Uses an RRDB generator, PatchGAN discriminator, and perceptual loss to improve image quality, evaluated using PSNR and SSIM metrics.
